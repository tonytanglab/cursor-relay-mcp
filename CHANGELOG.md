# Changelog

## 0.1.1

- 2026-09-03 15:35：修复进度显示层故障被误认为任务挂掉的问题：启动/续接工具不再自动创建 MCP 沙箱，新增 `open_run` 本机单任务令牌链接与 `read_run_progress` 纯快照接口，拒绝跨站与写操作，保留 24 小时任务总预算；内嵌面板修复首次读取失败停刷、终态事件不补读、初始化错误不可见、桥接无超时和关闭不清理等缺陷，优先标准桥接并支持重试，事件内存限制为最近 200 条且保持文本连续展示。同步修正默认提示超过 128 字符及 doctor 的过期 SDK 版本说明，补充安全、恢复、生命周期测试与沙箱握手故障排查文档；不修改宿主沙箱、不重发或取消既有任务。
- 2026-09-02 22:57：修复 Codex 插件升级后旧任务先能执行 `doctor` / `list_models`，首次 `agent.send` 却因旧缓存已清理而缺少 Cursor SDK 懒加载数字分块、继而出现 `Transport closed` 的生命周期故障：MCP 接受请求前通过 SDK 公共 `CursorAgentPlatform.prewarmLocalWorkspace()` 预载并释放本地执行器，以本地必失败的无网络模型目录探针预载云目录分支，使 Webpack 运行时在旧缓存目录被清理后仍可完成存量调用；新增真实缓存分块删除回归测试、启动前完整性检查、包校验及旧任务恢复说明。
- 2026-09-02 22:48：补充 Codex MCP App `resources/read ... Transport closed` 的分层诊断与恢复流程：明确区分旧任务 transport 被回收/重载与新任务启动失败，要求用 `doctor`、`list_models` 和面板资源读取交叉验收；记录插件更新后旧任务不会热加载、不得因面板失败重复提交 Cursor 运行，以及应凭原 `relayRunId` / `idempotencyKey` 恢复，避免把通道错误误判为模型结论或仓库损坏。
- 2026-09-01 09:35：统一 Cursor Relay 的只读与读写任务契约：`start_run` / `reply_run` 只接收获授权工作区、工作区内相对 `targetLocations` 与审查/实现/验收范围，禁止在 `task`、位置列表或其他 MCP 参数中嵌入源码正文、代码块、文件内容与补丁；Relay 在启动前执行服务端校验，再构造提示要求 Cursor 自行读取获授权文件，续接时可继承父运行的位置。同步收紧对话授权说明、持久状态、MCP schema、插件 Skill/默认提示、双语文档及只读/读写回归测试。
- 2026-09-01 09:10：按 Codex 长任务策略将 Cursor 运行的默认总预算与服务端硬上限统一为 24 小时（`86400000ms`）：普通 `start_run` / `reply_run` 省略 `timeoutMs` 即获得完整预算，环境配置只允许缩短而不能突破 24 小时；MCP 工具 schema、插件默认提示、Skill、`doctor` 冒烟断言、配置边界测试及中英文文档同步明确该限制，避免仍正常推进的任务在 2 小时或 4 小时被 Relay 误判超时。
- 2026-08-31 19:17：修复实时运行面板把同一段 Cursor 流式文本拆成大量带序号、身份和时间的独立事件卡片的问题：连续同身份的 `thinking` / 纯文本 `assistant` 事件现在投影为一个稳定文本块并原位增量更新，只在工具调用、结构化消息或身份变化等真实边界处分段；文本块不再重复展示底层事件序号与时间，结构化事件仍保留诊断信息，同时避免用户向上滚动查看历史时被新事件强制拉回底部。补充文本聚合与边界回归测试。
- 2026-08-31 08:23：修复 Codex 侧长时间 Cursor 任务在 30 分钟硬截止附近仍有活动事件却被 Relay 主动终止、短暂 SDK 断连表现为工具失败且缺少稳定进度入口的问题：默认总运行预算由 30 分钟提升为 2 小时并保留 4 小时上限；`wait_run` / `read_events` 在截止前遇到可重试重连错误时返回非终态 `connection.state=reconnecting`，不再固化运行失败；正式纳入绑定 `start_run` / `reply_run` 的 MCP Apps 只读实时面板和可重复打开的 `view_run`，展示截止时间、剩余预算、最近活动、事件与重连状态。同步恢复并持久化 Codex 控制工具策略、开放官方联网工具、补充 doctor 能力声明、插件 Skill、双语文档、MCP/配置/恢复/状态回归测试和安装说明。

## 0.1.0

- 2026-08-29 23:21：修复 Cursor 桌面端为 Pro、但 SDK stored login 指向另一账户时 Relay 只能误报套餐不足且无法自助恢复的问题：升级并锁定 `@cursor/sdk` 1.0.30，`doctor` 增加非敏感账户邮箱与后端诊断，新增需显式确认的 `reauthenticate_cursor` 浏览器重登录工具，模型目录将 `plan_required` 映射为稳定错误并提示复验；全链不读取、不返回新生成的 API key，并补充 adapter、缓存失效、MCP 工具与文档回归。
- 2026-08-28 17:54：修复有界 SDK 操作将唯一截止计时器从事件循环解除引用、导致底层 Promise 永不收敛时调用进程可能提前退出的问题；截止计时器现保持活动直至操作完成或保护期限到达，并恢复显式取消及过期恢复回归测试。
- 2026-08-28 16:22：将插件 `interface.defaultPrompt` 收敛到 Codex 128 字符上限以内，避免新会话启动时忽略默认提示；完整委派、安全与验收规则继续由包内 Skill 提供。
- 2026-08-28 16:11：修复 Cursor SDK 流式消息被按单字或极短片段持久化、导致工作流程难读且事件环形缓冲过快淘汰的问题：连续同运行的纯文本 `thinking` 与无工具块 `assistant` 现按段落边界聚合，并以 6 KiB UTF-8 文本和 8 KiB 完整事件双重上限安全分段；工具调用、状态、身份变化、未知字段及流异常继续构成严格边界。同步让显式取消最多等待 SDK 一秒，并仅以 250ms 有界窗口排空正常收敛的后台监控，避免 Windows 状态锁残留且不被失联监控长期阻塞；保留迟到的 `finished/error` 终态，并新增中文码点、结构化边界、异常刷新、迟到拒绝及不收敛取消回归测试。
- 2026-08-28 13:19：修复持久运行恢复与超时收敛边界：截止时间前的可重试 `getRun` 错误不再把运行过早固化为失败；超时取消前补捕已完成终态，取消后继续保留迟到的 `finished/error` 结果与助手文本，同时在监控或过期流程退出时始终幂等释放 SDK handle。新增临时网络错误二次恢复、取消与等待均不收敛、终态快照先于迟到等待信号的回归测试。
- 2026-08-27 15:27：将白名单外交互授权从“五分钟一次性只读”升级为绑定当前 MCP 对话与规范化工作区的可复用 capability，支持同一对话内多次 `start_run` / `reply_run` 读写；写授权可降级只读、只读授权不可提权，跨对话及 `danger-full-access` 继续拒绝，令牌仍不落盘且随作用域或进程结束失效。同步让 Windows 的 `workspace-write` 采用 SDK sandbox 兼容钳制，并更新 Codex Skill/默认提示，要求 Cursor 自行读取和修改工作区、报告变更文件，Codex 通过 `wait_run`、`read_events` 与本地变更清单跟踪和复核进度。
- 2026-08-27 14:26：补充 Codex 插件内置 MCP 生成与调用说明，明确 manifest 通过 `mcpServers` 引用包内 `.mcp.json`、相对 `cwd` 和入口解析规则，禁止硬编码版本缓存或重复注册同名 MCP；Skill 同步区分“Cursor 自行读取工作区”与“Codex 上传源码”，并把明确实施请求映射为 `workspace-write`，对白名单外现行限制给出准确错误语义。
- 2026-08-27 00:49：修复 Windows 状态文件原子替换遇到 `EPERM/EACCES/EBUSY` 时导致运行悬挂的问题：新增覆盖完整读改写事务的跨进程锁、活进程保护与 ABA 安全的陈旧锁回收、原子替换有界退避、临时文件清理，以及读写/锁错误的结构化分类和确定性并发回归测试。
- 2026-08-26 19:11：将 Windows 只读 SDK 沙箱保护从平台默认值升级为强制兼容钳制，即使遗留环境显式设置为 `true` 也不会再进入当前 Cursor SDK 不支持的本地沙箱路径；非 Windows 平台仍可显式关闭沙箱。
- 2026-08-26 18:55：修复 Windows 上 Cursor SDK 本地沙箱不受支持却仍被默认强制启用的问题；只读预设改为平台自适应沙箱默认值，Windows 关闭 SDK 沙箱但继续锁定 `read`、`grep`、`glob`、`ls` 工具白名单，并保留显式环境覆盖。
- 2026-08-26 18:07：新增 Codex Windows 快速安装与避坑手册，记录 personal marketplace 的真实路径解析、WindowsApps CLI 备用入口、Cursor stored login、SDK sandbox 兼容、缓存重装及 Grok 4.6 真实调用验收流程，并加入双语 README 与打包清单入口。
- 2026-08-26 14:48：修正交互式工作区授权协议：新增 `authorize_workspace`，允许用户在当前对话明确授权白名单外的精确只读任务；capability 五分钟有效、一次消费并绑定真实路径、任务、幂等键与 MCP 调用范围，令牌不落盘，写入及危险权限仍要求静态白名单。
- 2026-08-26 08:14：根据真实 Cursor SDK 验收补充只读沙箱兼容开关，默认保持安全失败；关闭时仍锁定只读工具白名单。同时规范化空白 `CURSOR_API_KEY`，确保官方 stored login 回退不被空变量阻断。
- 2026-08-25 22:39：采纳 Harness K3 MAX 实现复审结论，恢复截止时间的终态宽限与取消后有界收敛，允许官方目录中的合法非预设参数组合，并避免过截止恢复歧义反复楔住。
- 2026-08-25 22:15：基于官方 `@cursor/sdk` 公开合约完成安全升级：新增危险权限双门、启动单飞、分页歧义恢复、顺序流消费、终态资源释放、stored login、深状态校验、模型规范化与缓存、紧凑事件摘要及结构化 SDK 错误，并补充完整回归测试与双语文档。
- 2026-08-25 21:13：采纳 Harness K3 MAX 审计结论，补强后台异常收敛、启动崩溃窗口恢复、终态 CAS、截止时间宽限与 POSIX 符号链接入口，并新增对应回归测试。
- 2026-08-25 20:07：创建基于 `@cursor/sdk` 1.0.28 的 Cursor Relay MCP，提供模型发现、权限映射、幂等运行、持久恢复、等待/续接/取消、事件读取、结构化错误、Codex 插件与完整测试脚手架。
